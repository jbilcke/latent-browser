import { NoopComponent } from '../../../common'
import { Component } from '../../../types'

export const js: Component = {
  component: NoopComponent,
  description: 'JS one-liner expression passed to eval()',
}
